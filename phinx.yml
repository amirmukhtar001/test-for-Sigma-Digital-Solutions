paths:
    # migrations: "%PHINX_CONFIG_DIR%/db/migrations"
    migrations: db/migrations
environments:
    development:
        adapter: mysql
        host: localhost
        name: ecommerce-store
        user: root
        pass: ''
        port: 3306
        charset: utf8
